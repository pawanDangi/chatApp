import React from 'react';
 
// Footer component

const Setting = () => (
  <div className="IM-page">
    <span>Instant messaging will be available shortly.</span>
  </div>
);

export default Setting;
